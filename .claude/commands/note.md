# Obsidian 노트 생성

사용자의 요청에 따라 이 Obsidian vault에 새 노트를 생성한다.

## 입력

$ARGUMENTS

인자 형식: `<주제영역> <노트제목>` (예: `gcp Cloud Run 서버리스`, `rust 에러 핸들링`)

## 노트 생성 규칙

### 1. 파일 위치 및 이름 결정

주제영역에 따라 디렉토리와 파일명 접두사를 결정:

| 주제 | 디렉토리 | 파일명 패턴 | 예시 |
|------|---------|-----------|------|
| `git` | `MZ-CLOUD/git/` | `git-<topic>.md` | `git-stash.md` |
| `rust` | `MZ-CLOUD/rust/` | `rust-<topic>.md` | `rust-ownership.md` |
| `gcp` | `MZ-CLOUD/gcp/` | `gcp-<topic>.md` | `gcp-cloud-run.md` |
| `docker` | `MZ-CLOUD/docker/` | `docker-<topic>.md` | `docker-compose.md` |
| `k8s` | `MZ-CLOUD/kubernetes/` | `kubectl-<topic>.md` | `kubectl-apply.md` |
| `github` | `MZ-CLOUD/github/` | `gha-<topic>.md` | `gha-workflow.md` |
| `gitlab` | `MZ-CLOUD/gitlab/` | `gitlab-<topic>.md` | `gitlab-jobs.md` |
| `aws` | `MZ-CLOUD/aws/` | `aws-<topic>.md` | `aws-s3.md` |
| `cicd` | `MZ-CLOUD/cicd/` | `cicd-<topic>.md` | `cicd-pipeline.md` |

비교 노트는 `<prefix>-<x>-vs-<y>.md` 형식 (예: `git-merge-vs-rebase.md`).
크레이트/라이브러리 노트는 `<prefix>-crate-<name>.md` 형식 (예: `rust-crate-tokio.md`).

### 2. 노트 구조

모든 노트는 다음 구조를 따른다:

```markdown
# 제목 (한국어)

#태그1 #태그2 #태그3

---

한 줄 설명문

> 관련 문서: [[관련노트1]], [[관련노트2]]

## 섹션 제목

### 특징/개요

| **항목** | **설명** |
|---------|---------|
| ... | ... |

### CLI (명령어가 있는 경우)

\`\`\`bash
명령어 예시
\`\`\`

### 웹 콘솔 (GCP/AWS 등 클라우드 서비스인 경우)

> 📍 메뉴 경로

1. **필드명**: `값` 입력
2. ...
```

### 3. 콘텐츠 규칙

- **언어**: 모든 내용을 한국어로 작성
- **링크**: Obsidian wiki-link `[[노트이름]]` 사용
- **태그**: 한국어 해시태그 사용 (`#git #커밋 #브랜치`)
- **태그 위치**: `# 제목` 바로 아래
- **테이블**: 명령어나 옵션이 있으면 테이블로 정리
- **코드블록**: 명령어는 `bash`, 설정은 `yaml`/`json` 등 적절한 언어 지정
- **클라우드 서비스**: CLI와 웹 콘솔 양쪽 모두 제공

### 4. MOC 업데이트

노트 생성 후, 해당 주제의 MOC 파일에 새 노트 링크를 추가:
- `Git_MOC.md`, `Rust_MOC.md`, `GCP_MOC.md`, `CICD_MOC.md` 등
- MOC 파일이 존재하면 적절한 섹션에 `[[새노트]]` 링크 추가

### 5. 관련 노트 확인

노트 생성 전에 같은 디렉토리의 기존 노트를 확인하여:
- 중복 노트가 있는지 확인
- 관련 노트를 `> 관련 문서:` 섹션에 링크
- 기존 노트에서도 새 노트로의 역링크 추가 (필요 시)

## 실행

1. 인자를 파싱하여 주제영역과 노트 제목을 추출
2. 해당 디렉토리의 기존 노트를 확인 (중복/관련 노트)
3. 파일명과 경로를 결정
4. 노트 내용을 생성하여 파일로 작성
5. MOC 파일이 있으면 링크 추가
6. 생성된 노트 경로와 요약을 사용자에게 보고
