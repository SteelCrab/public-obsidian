# =============================================================================
# MySQL Replication 환경 변수
# =============================================================================
# 사용법: source .env && ./init-replication.sh
# 주의: 이 파일을 .gitignore에 추가하세요
# =============================================================================

# MySQL Master (VM Docker)
MYSQL_PRIMARY_HOST=172.100.100.11
MYSQL_ROOT_PASSWORD=your_password

# Replication 사용자
MYSQL_REPL_USER=repl_pista
MYSQL_REPL_PASSWORD=your_repl_password

# Kubernetes
NAMESPACE=gition
