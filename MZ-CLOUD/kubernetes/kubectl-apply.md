# Kubectl 리소스 적용

#kubernetes #kubectl #apply

---

선언적 리소스 관리.

| 명령어 | 설명 |
|--------|------|
| `kubectl apply -f <파일>` | 파일 적용 |
| `kubectl apply -f <디렉토리>/` | 디렉토리 적용 |
| `kubectl apply -f <URL>` | URL에서 적용 |
| `kubectl apply -k <디렉토리>/` | Kustomize 적용 |
| `kubectl diff -f <파일>` | 변경사항 미리보기 |
